import { Routes } from '@angular/router';
import { SimuladorEscalonamentoComponent } from './simulador-escalonamento/simulador-escalonamento';

export const routes: Routes = [
  { path: '', component: SimuladorEscalonamentoComponent },
];